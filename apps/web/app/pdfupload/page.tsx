import PdfUpload from "../components/PdfUpload";

export default function page() {
  return (
      <main className="min-h-screen bg-gradient-to-b from-slate-800 to-slate-400 text-white p-22">
        <PdfUpload/>
      </main>
  );
}